import 'package:flutter/material.dart';

class CheckInternetConectionService {
  showNoInternetBottomSheet(BuildContext context) {
    showModalBottomSheet(
      context: context,
      builder: (context) {
        return Column(
          children: [],
        );
      },
    );
  }
}
